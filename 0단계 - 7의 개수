import Foundation

func solution(_ array:[Int]) -> Int {
    var array2 = ""
    var count = 0
    
    for i in array{
      array2 += String(i)
    }
     var result = array2.map{String($0)}
    
    for i in result{
        if i == "7"{
            count += 1
        }
    }
    return count
}
