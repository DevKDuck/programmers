import Foundation

func solution(_ order:Int) -> Int {
    
    let orderArray = String(order).map{String($0)}
    var result = 0
    
    for num in orderArray{
        if num == "3" || num == "6" || num == "9"{
            result += 1
        }
    }
    
    return result
}
